### Usage
- install required packages with pipenv command --dev
```
pipenv install --dev
```
- run each script with pipenv command
```
pipenv run start
pipenv run index
pipenv run document
pipenv run search
```
 
